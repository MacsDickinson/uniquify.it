@{
    ViewBag.Title = "title";
    Layout = "../Shared/_Layout.cshtml";
}
@section scripts {
    <script type="text/javascript">
        $(function() {
            var securePass = new SecureClass();
            $('#getMemorable').click(function () {
                generatePass('memorable');
            });
            $('#getSuper').click(function () {
                generatePass('secure');
            });
            var generatePass = function (type) {
                $('#securepass-result').text('');
                $('.field-validation-error').hide();
                var domain = $('#ServiceName').val();
                var key = $('#SecretWord').val();
                if (domain.length === 0) {
                    $('#domain-val').text('Please enter a domain');
                    $('#domain-val').show();
                    return;
                }
                if (key.length > 7) {
                    var result;
                    if (type === 'secure') {
                        result = securePass.generateSecure(domain, key);
                    } else {
                        result = securePass.generateMemorable(domain, key);
                    }
                    $('#securepass-result').text(result);
                } else {
                    $('#key-val').text('Secret word needs to be at least 8 characters');
                    $('#key-val').show();
                }
            };
        });
    </script>
}
<header>
    <h1>SecurePass</h1>
</header>
<div class="form-container">
    <h3>Get Your Secure Password:</h3>
    <div class="fm-q">
        <label for="ServiceName">Service Name:</label>
        <input id="ServiceName" name="ServiceName" placeholder="eg. google.com" type="text" value="">
        <span class="field-validation-error" id="domain-val"></span>
    </div>
    <div class="fm-q">
        <label for="SecretWord">Secret Word:</label>
        <input id="SecretWord" name="SecretWord" type="text" value="">
        <span class="field-validation-error" id="key-val"></span>
    </div>
    <div class="fm-actons">
        <input type="submit" id="getMemorable" value="Get Secure Password" />
        <input type="submit" id="getSuper" value="Get Super Secure Password" />
    </div>
</div>
<div class="answer-container">
    <h2 id="securepass-result"></h2>
</div>